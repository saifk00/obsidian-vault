An elastic circuit implements some dataflow logic along with control signals (ready/valid signals). This creates a *control and dataflow graph*, or **CDFG**, which is then lowered into a circuit [[borup-2022]].



